import React from "react";
import { ScrollView, Text } from "react-native";

const Account = () => {
  return (
    <ScrollView className="bg-white flex flex-grow">
      <Text>account</Text>
    </ScrollView>
  );
};

export default Account;
